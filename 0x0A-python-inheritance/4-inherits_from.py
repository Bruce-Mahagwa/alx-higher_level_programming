#!/usr/bin/python3
"""Contains the module 4-inherits_from"""


def inherits_from(obj, a_class):
    """checks if an object is an instance of a class"""
    return issubclass(type(obj), a_class) and type(0
